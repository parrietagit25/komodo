# API base URL (no trailing slash); /api is appended by the app
# - Con HTTPS local (certificado en komodo_frontend/certs/): usar VITE_API_URL=/api (proxy en Vite)
# - Sin HTTPS, acceso por red: usar IP del backend, ej. VITE_API_URL=http://10.10.2.115:8001/api
VITE_API_URL=http://localhost:8000/api
