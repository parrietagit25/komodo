# Django
DEBUG=True
DJANGO_SECRET_KEY=your-secret-key-here
# Incluir IP o hostname del servidor para acceso desde la red (ej. 10.10.2.115)
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (5433 por defecto si 5432 está ocupado por otro contenedor)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# Backend web (8001 por defecto si 8000 está ocupado)
WEB_PORT=8001

# Frontend (8080 por defecto)
FRONTEND_PORT=8080
POSTGRES_DB=komodo_db
POSTGRES_USER=komodo
POSTGRES_PASSWORD=komodo

# CORS (comma-separated in production)
# CORS_ALLOWED_ORIGINS=https://app.example.com
